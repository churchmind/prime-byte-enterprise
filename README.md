# Prime Byte Enterprise website

This repository contains the supplied static HTML pages for the Prime Byte Enterprise website and a GitHub Actions workflow for GitHub Pages deployment.

The upload included the HTML documents only. Those pages reference additional `css/`, `js/`, and `images/` files; add those directories to the repository to restore the original styling, scripts, and imagery completely.
